<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.msemyak.carsdb">

    <application
        android:name=".CarManagerApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_car"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round_car"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".activities.main.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activities.add_owner.AddEditOwnerActivity">
        </activity>
        <activity android:name=".activities.owner_details.OwnerDetailsActivity">
        </activity>
        <activity android:name=".activities.add_car.AddEditCarActivity">
        </activity>
        <activity android:name=".activities.car_details.CarDetailsActivity">
        </activity>
    </application>

</manifest>